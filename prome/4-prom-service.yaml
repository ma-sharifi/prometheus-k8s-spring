---
apiVersion: v1
kind: Service
metadata:
  name: myapp-prom-service
  namespace: staging
  labels:
    app: myapp-monitoring
spec:
  type: ClusterIP
  ports:
    - name: http-metrics
      port: 8080
      targetPort: http-metrics
  selector:
    app: myapp
